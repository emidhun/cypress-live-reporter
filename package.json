{
  "name": "cypress-live-reporter",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted live Cypress test reporting — streams run/test events and failure evidence to Postgres or a webhook",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tools/cypress-live-reporter/test/smoke.js",
    "test:pg": "node tools/cypress-live-reporter/test/integration-pg.js"
  }
}
